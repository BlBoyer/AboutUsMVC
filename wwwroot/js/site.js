// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.
/*function validatePassword(_password) {
  //this input.onchange
  const PASSEX = /^[A-Za-z]\W+(7)/;
  if (_password.match(PASSEX)) {
    return true;
  } else {
    return "Password must start with a letter, and be at least 8 characters long.";
  }
  //check if password is of minimum length we can do this in regex though
}
$("#mixedForm").validate({
  rules: {
    '_username': {
      required: true,
      minlength: 5
      },
    '_password': {
    required: true,
    minlength: 8,
    pattern: /^A-Za-z\W+/
    }
  }
})</script>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
*/